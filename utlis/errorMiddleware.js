errorLogger = async (err, req, res, next) => {
    console.log(err + " ??????????????????????????????????????????????????");
    const message = err.message

    res.status(500).send({ message, err })
}

module.exports = { errorLogger }
